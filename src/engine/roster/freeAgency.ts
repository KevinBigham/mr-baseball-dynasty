/**
 * Free agency bid modifiers.
 * Stub — Sprint 04 branch surgery.
 */

export interface TeamBidModifier {
  bidAggression: number;
  offerMultiplier: number;
  payrollTarget: number;
  maxPayrollCap: number;
  luxuryTaxComfort: number;
}
